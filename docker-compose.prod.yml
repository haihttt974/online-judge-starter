services:
  judge-app:
    build: .
    container_name: judge-app-prod
    ports:
      - "80:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
      - MAX_OUTPUT_BYTES=1048576
